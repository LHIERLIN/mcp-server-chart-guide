# 🚀 MCP Server Chart 快速參考卡

> 一頁速查所有圖表類型和基本用法

---

## 📊 圖表類型速查表

### 基礎圖表

| 圖表 | 工具 | 數據格式 | 適用場景 |
|------|------|----------|----------|
| 📈 折線圖 | `generate_line_chart` | `[{time, value, group?}]` | 趨勢變化 |
| 📊 柱狀圖 | `generate_column_chart` | `[{category, value, group?}]` | 分類對比 |
| 📉 橫向柱狀圖 | `generate_bar_chart` | `[{category, value, group?}]` | 水平對比 |
| 🥧 餅圖 | `generate_pie_chart` | `[{category, value}]` | 比例分布 |
| 📈 面積圖 | `generate_area_chart` | `[{time, value, group?}]` | 累積趨勢 |

### 統計圖表

| 圖表 | 工具 | 數據格式 | 適用場景 |
|------|------|----------|----------|
| 🔵 散點圖 | `generate_scatter_chart` | `[{x, y, group?}]` | 相關分析 |
| 🎯 雷達圖 | `generate_radar_chart` | `[{name, value, group?}]` | 多維評估 |
| 📊 直方圖 | `generate_histogram_chart` | `[number, ...]` | 分布頻率 |
| 📦 箱線圖 | `generate_boxplot_chart` | `[{category, value, group?}]` | 統計摘要 |
| 🎻 小提琴圖 | `generate_violin_chart` | `[{category, value, group?}]` | 分布密度 |

### 關係圖表

| 圖表 | 工具 | 數據格式 | 適用場景 |
|------|------|----------|----------|
| 🧠 思維導圖 | `generate_mind_map` | `{name, children[]}` | 知識結構 |
| 🏢 組織圖 | `generate_organization_chart` | `{name, description?, children[]}` | 層級結構 |
| 🔀 流程圖 | `generate_flow_diagram` | `{nodes[], edges[]}` | 流程步驟 |
| 🕸️ 網絡圖 | `generate_network_graph` | `{nodes[], edges[]}` | 關係網絡 |
| 🐟 魚骨圖 | `generate_fishbone_diagram` | `{name, children[]}` | 因果分析 |

### 進階圖表

| 圖表 | 工具 | 數據格式 | 適用場景 |
|------|------|----------|----------|
| 🔻 漏斗圖 | `generate_funnel_chart` | `[{category, value}]` | 轉化分析 |
| 🌊 桑基圖 | `generate_sankey_chart` | `[{source, target, value}]` | 流量流向 |
| 💹 瀑布圖 | `generate_waterfall_chart` | `[{category, value?, isTotal?}]` | 累積變化 |
| 📊 雙軸圖 | `generate_dual_axes_chart` | `{categories[], series[]}` | 雙量級對比 |
| 🌳 樹狀圖 | `generate_treemap_chart` | `[{name, value, children[]}]` | 層級占比 |
| ⭕ 韋恩圖 | `generate_venn_chart` | `[{value, sets[], label?}]` | 集合關係 |

### 特殊圖表

| 圖表 | 工具 | 數據格式 | 適用場景 |
|------|------|----------|----------|
| 💧 水波圖 | `generate_liquid_chart` | `{percent: 0-1}` | 進度展示 |
| ☁️ 詞雲圖 | `generate_word_cloud_chart` | `[{text, value}]` | 詞頻展示 |

### 地圖

| 圖表 | 工具 | 數據格式 | 適用場景 |
|------|------|----------|----------|
| 🗺️ 區域地圖 | `generate_district_map` | `{name, subdistricts[]}` | 區域分布 |
| 📍 標記地圖 | `generate_pin_map` | `["地點名稱", ...]` | 位置標記 |
| 🛤️ 路線地圖 | `generate_path_map` | `[{data: ["地點", ...]}]` | 路線規劃 |

---

## ⚙️ 通用參數

```
width: 600        # 寬度（像素）
height: 400       # 高度（像素）
title: ""         # 標題
theme: "default"  # 主題: default | academy | dark
style:
  texture: "default"  # 樣式: default | rough (手繪)
  backgroundColor: "#fff"
  palette: ["#color1", "#color2", ...]
```

---

## 📝 常用數據格式範本

### 時間序列
```json
[
  {"time": "2024-01", "value": 100},
  {"time": "2024-02", "value": 150}
]
```

### 分類數據
```json
[
  {"category": "A", "value": 100},
  {"category": "B", "value": 200}
]
```

### 分組數據
```json
[
  {"category": "A", "value": 100, "group": "Group1"},
  {"category": "A", "value": 80, "group": "Group2"}
]
```

### 層級結構
```json
{
  "name": "根節點",
  "children": [
    {"name": "子節點1"},
    {"name": "子節點2", "children": [...]}
  ]
}
```

### 節點連線
```json
{
  "nodes": [{"name": "A"}, {"name": "B"}],
  "edges": [{"source": "A", "target": "B", "name": "標籤"}]
}
```

---

## 🎨 主題預覽

| 主題 | 特色 |
|------|------|
| `default` | 清新淺色，適合日常使用 |
| `academy` | 學術風格，適合報告演示 |
| `dark` | 深色主題，適合深色背景 |

---

## 💬 快速指令範例

**生成折線圖**
```
請生成折線圖，標題「銷售趨勢」：
1月:100, 2月:150, 3月:200
```

**生成餅圖**
```
請生成餅圖，顯示市場份額：
A:40%, B:30%, C:30%
```

**生成流程圖**
```
請生成流程圖：
開始 → 處理 → 判斷 → (Yes)成功 → 結束
                  ↓(No)
               重試 → 判斷
```

**生成組織圖**
```
請生成組織圖：
CEO
├── CTO
│   ├── 開發部
│   └── 測試部
└── CFO
```

---

## ⚡ 技巧提示

1. **選擇圖表**：趨勢→折線圖，對比→柱狀圖，比例→餅圖
2. **手繪風格**：添加 `style.texture: rough`
3. **深色主題**：設置 `theme: dark`
4. **甜甜圈圖**：餅圖設置 `innerRadius: 0.6`
5. **分組對比**：數據添加 `group` 欄位

---

Made with ❤️ for quick reference
